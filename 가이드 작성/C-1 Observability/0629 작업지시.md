# B-1,2,3 양식을 참조
> 전반적으로 이 가이드 특성상 필요한 내용이 아닐 경우 B-1,2,3에 없는 것을 포함하지 않는다. 문장 구조나 어투, 배치 위치 최대한 B-1,2,3 스타일에 맞춰서.

# 윗단 시작 부분

제목 - 목차 - 관련 가이드 - 머릿말 (이 주제는 무엇이며.. 이 가이드는 무엇 무엇을 설명한다) - 이 가이드가 답하는 5가지 질문 (표 형식) - 그리고 1장으로


# 목차 수정

Why --> What --> When (있을 시) --> How --> Tech Stack --> Where 로 유지

없는 섹션은 포함 안해도 무방, 하지만 Why What How Tech Stack은 필수로 포함하며 순서도 동일하게 유지함


제목에 "(한눈에)" 이런 문구 삭제

각 장의 제목에 촘촘히, 탓인지, '+' 등의 문구 및 기호 사용 금지, - 또는 · 만 유일하게 제목에서 허용


윗단의 "예시 표기 안내" --> 삭제

현재:

## 4. 예시 시나리오 (한눈에)
### 4.1 적용 전과 후
### 4.2 흐름 미리보기

수정:

## 4. How — 어떻게 준비·운영하나
### 4.1 적용 전후 비교
### 4.2 구축 절차

How를 다음과 같이 변경 (예시 시나리오 -> How로 흡수) 
## 4. How — 어떻게 준비·운영하나 ### 4.1 적용 전후 비교 ### 4.2 Observability 구축 절차 ### 4.3 관측 지표·판단 기준 설정 ### 4.4 알림·에스컬레이션 운영 ### 4.5 운영 성과 측정

8. KPI - 성과지표를 --> 4.7 운영 성과 측정으로 빼고

KPI 항목은:
- 이상 감지 시간 (MTTD)
- 해결시간 (MTTR)
- 놓친 건수
- 오탐률
- 관측 커버리지

로 유지하되, 산식이 산정 가능한 경우 표에 칼럼을 추가


문체수정: 보고서 톤 사용

막히는 지점, 값이 튀어도, 조용히 망가진다, 엉뚱한 곳을 뒤지는 시간, 누가 볼 것인지
--> 이런 워딩 금지, 대신 현업 적용 시 발생하는 문제, 이상값이 유입됨, 운영 중 비정상 상태가 발생함, 원인 분석 시간이 증가함, 담당 책임이 불명확함 등의 보고서 톤 사용


# 상세 문구 수정

# 1장

현재: 데이터는 한 번 잘 만들어 두면 끝나는 자산이 아니다. 운영에 들어가면 수집이 끊기고, 건수가 튀고, 원천 시스템이 바뀌면서 조용히 망가진다.

수정: 데이터는 구축 이후에도 지속적인 운영과 관리가 필요한 자산이다. 운영 과정에서는 수집 지연, 적재 누락, 건수 변화, 원천 시스템 변경 등으로 데이터 흐름에 비정상 상태가 발생할 수 있다.

현재: | 막히는 지점 | 현장에서 벌어지는 일 |

수정: | 발생 문제 | 현업 영향 |

현재: 멈춘 줄 모른 채 옛 데이터로 답함

수정: 수집 중단을 늦게 인지함

현재: 값이 튀어도 그대로 흘러감

수정: 이상값이 그대로 적재됨

현재: 원천이 바뀌면 조용히 어긋남

수정: 원천 변경으로 하위 입력 오류 발생

현재: 이상이 나도 누구 일인지 불명확

수정: 담당 책임이 불명확함

현재: AI 답이 틀린 원인을 못 가림

수정: AI 결과 오류의 원인 구분이 어려움

현재: 이상을 사람보다 먼저, 자동으로 잡는다.

수정: 운영 중 이상 징후를 자동으로 감지한다.

현재: 원인을 빨리 좁힌다.

수정: 원인 후보를 빠르게 구분한다.

현재: 이번엔 데이터 탓인지 아닌지를 먼저 가려 엉뚱한 곳을 뒤지는 시간을 없앤다.

수정: 데이터, 전처리, 모델, Prompt 중 원인 영역을 우선 구분하여 원인 분석 시간을 단축한다.

# 2장

현재: 2.1 데이터 Observability란 + 체계 내 위치

수정: 2.1 데이터 Observability의 역할과 체계 내 위치

현재: AI 결과가 이상할 때 가장 먼저 부딪히는 질문은 "데이터가 문제인가, 모델이 문제인가, Prompt가 바뀐 탓인가"이다.

수정: AI 결과가 비정상일 때 가장 먼저 확인해야 하는 것은 데이터 문제인지, 모델 문제인지, Prompt 변경의 영향인지 구분하는 것이다.

현재: 이 두 갈래만 갈라도 어디를 들여다볼지가 정해진다.

수정: 이 구분이 가능해야 데이터 파이프라인, 모델, Prompt 가운데 어떤 영역을 우선 점검할지 결정할 수 있다.


# 3장

3.2 제목 수정

현재:  3.2 더 촘촘히 볼 대상

수정: 3.2 관측 우선순위 판단 기준

현재: 변동이 잦거나 멈추면 타격이 큰 흐름으로 촘촘하게 넓혀 간다.

수정: 변동이 잦거나 중단 시 업무 영향이 큰 흐름으로 관측 범위를 확대한다.

현재: 기본 대상에 관측을 건 다음에는 변동이 잦거나 멈추면 타격이 큰 흐름을 더 촘촘히 본다.

수정: 기본 대상에 관측을 적용한 이후에는 업무 영향도와 변동 수준을 고려하여 관측 수준을 단계적으로 높인다.

표 수정

즉시 · 촘촘히 --> 즉시 적용

Mermaid 수정

최우선 · 촘촘히 --> 최우선 관측

# 상세 문구 수정

이 가이드가 답하는 5가지 질문
신선도·양·구조·값 분포 네 축을 정해 두고 각 축마다 지표를 건다

-->

신선도·양·구조·값 분포 네 축을 정해 두고 각 축마다 지표를 설정한다

2.1 데이터 Observability의 역할과 체계 내 위치
데이터 Observability는 운영 중인 데이터 흐름의 상태를 지속적으로 지켜보다가, 지연·누락·구조 변경·값 이상 같은 징후가 나타나면 그 즉시 감지해 담당자에게 알리는 체계다. 공장의 관제실에 비유할 수 있다. 관제실은 설비를 직접 고치지는 않지만, 어느 라인이 멈췄고 어느 계기가 비정상인지를 한눈에 비추고 경보를 울려, 사람이 제때 손쓰게 한다. 데이터 관측도 데이터를 직접 고치거나 사용 여부를 판정하지는 않는다. 흐름이 정상인지 지켜보고 이상을 알리는 데까지가 책임 범위다.

-->

데이터 Observability는 운영 중인 데이터 흐름의 상태를 지속적으로 지켜보다가, 지연·누락·구조 변경·값 이상 같은 징후가 나타나면 그 즉시 감지해 담당자에게 알리는 체계다. 이를 공장의 관제실에 비유하면, 관제실은 설비를 직접 고치지는 않지만, 어느 라인이 멈췄고 어느 계기가 비정상인지를 한눈에 비추고 경보를 울려, 사람이 제때 손쓰게 한다. 마찬가지로 데이터 관측은 데이터를 직접 고치거나 사용 여부를 판정하는 것이 아닌, 흐름이 정상인지 지켜보고 이상을 알리는 역할을 수행한다.

데이터 관측은 데이터를 신뢰할 수 있게(Trustworthy) 만드는 C 그룹에 속한다. C 그룹은 세 주제가 역할을 나눈다. 사용 여부를 판정하는 C-2 데이터 품질 관리가 상위의 관문이라면, C-1은 운영 중 실시간 신호로, C-3 데이터 계통 Lineage는 사후 이력으로 그 판정에 근거를 댄다. C-1이 보는 것은 "지금 이 순간 흐름이 정상인가"이고, 다른 주제와의 경계는 6장에서 정리한다.

-->

데이터 관측은 데이터를 신뢰할 수 있게(Trustworthy) 만드는 단계에 속한다. 사용 여부를 판정하는 C-2 데이터 품질 관리가 상위의 관문이라면, C-1은 운영 중 실시간 신호로, C-3 데이터 계통 Lineage는 사후 이력으로 그 판정에 근거를 뒷받침한다. C-1이 보는 것은 "지금 이 순간 흐름이 정상인가"이고, 다른 주제와의 경계는 6장에서 정리한다.

2.2 관측 4축 — 무엇을 지켜보나
관측은 막연히 "데이터를 본다"가 아니라, 흐름이 망가지는 네 가지 길목을 정해 두고 그 길목마다 지표를 건다. 본 가이드는 이 네 가지를 관측의 정본 모델로 삼아 끝까지 일관되게 쓴다.

-->

관측은 단순히 “데이터를 본다”는 의미가 아니라, 데이터 흐름이 훼손되는 네 가지 주요 지점을 정의하고 각 지점에 대응하는 지표를 설정하는 것이다. 본 가이드는 이 네 가지를 관측의 정본 모델로 삼아 끝까지 일관되게 쓴다.

| 관측 축 | 무엇을 보나 | 이상 신호 (텔레매틱스 예시) |

-->

| 관측 축 | 관측 항목 | 이상 신호 (텔레매틱스 예시) |

2.3 이상 판단 기준 — 고정 한계값과 평소 추세
지표를 걸었으면 "어디까지가 정상이고 어디부터가 이상인가"를 정해야 한다. 판단 기준은 두 방식이 있고, 데이터 성격에 따라 골라 쓴다.

-->

지표를 설정했으면 "어디까지가 정상이고 어디부터가 이상인가"를 정해야 한다. 판단 기준은 두 방식이 있고, 데이터 성격에 따라 선택하여 사용한다.

평소 추세 대비(적응형 베이스라인, Adaptive Baseline). 과거의 요일·시간대·계절 패턴을 학습해 "이 시간대의 정상 범위"를 자동으로 잡고, 그 범위를 벗어날 때만 알린다. 변동이 큰 제조 데이터에 맞고, 임계값을 일일이 정하지 않아도 된다. 다만 학습 초기에는 정확도가 낮고, 왜 이상으로 봤는지 설명이 어려울 수 있다.

평소 추세(적응형 베이스라인, Adaptive Baseline). 과거의 요일·시간대·계절 패턴을 학습해 "이 시간대의 정상 범위"를 자동으로 잡고, 그 범위를 벗어날 때만 알린다. 변동이 큰 제조 데이터에 맞고, 임계값을 일일이 정하지 않아도 된다. 다만 학습 초기에는 정확도가 낮고, 왜 이상으로 봤는지 설명이 어려울 수 있다.

권장 — 평소 추세를 기본으로, 고정 한계값을 보강으로. 제조 데이터는 요일·교대·계절 변동이 커서 고정 한계값만으로는 오탐이 많다. 평소 추세를 기본으로 걸고, 물리적으로 불가능한 값(음수 두께, 오류코드)이나 규정 한계는 고정 한계값으로 덧대는 조합을 권장한다.

-->

권장 — 평소 추세를 기본으로, 고정 한계값을 보강으로. 제조 데이터는 요일·교대·계절 변동이 커서 고정 한계값만으로는 오탐이 많다. 평소 추세를 기본으로 설정하고, 물리적으로 불가능한 값(음수 두께, 오류코드)이나 규정 한계는 고정 한계값으로 덧대는 조합을 권장한다.

2.4 알림 체계 — 심각도 등급과 알림 대상
| 심각도 | 어떤 상황 | 알림 대상 | 경로·시점 |

-->

| 심각도 | 상황 | 알림 대상 | 경로·시점 |

2.5 원인 구분용 기록 — 데이터·모델 원인 구분
| 기록 항목 | 무엇을 남기나 | 확인 포인트 |

--> | 기록 항목 | 기록 내용 | 확인 포인트 |

3. When — 어디부터 관측하나 (관측 대상 우선순위)
관측은 기본적으로 모든 핵심 흐름에 필요하지만, 한 번에 전부 걸지는 않는다. AI 서비스로 바로 흘러가는 흐름부터 걸고, 변동이 잦거나 중단 시 업무 영향이 큰 흐름으로 관측 범위를 확대한다.

-->

관측은 기본적으로 모든 핵심 흐름에 필요하지만, 일괄 적용하지는 않는다. 우선 AI 서비스로 직접 연결되는 흐름부터 적용한 뒤, 변동이 잦거나 중단 시 업무 영향이 큰 흐름으로 관측 범위를 확대한다.

3.1 기본 필수 대상
가장 먼저 거는 대상은 AI 서비스와 핵심 리포트로 바로 흘러가는 흐름이다. 이 흐름이 멈추거나 틀어지면 곧장 잘못된 AI 결과·잘못된 보고로 이어지기 때문이다. 텔레매틱스 예시라면 장비 데이터 수집 파이프라인부터 그 데이터가 적재되는 피처 테이블, 예지보전 AI가 읽는 입력까지가 1순위다.

-->

우선적으로 관측해야 할 대상은 AI 서비스와 핵심 리포트로 직접 연결되는 흐름으로, 해당 흐름이 중단되거나 왜곡될 경우 곧바로 잘못된 AI 결과와 부정확한 보고로 이어질 수 있기 때문이다. 앞서 살펴본 텔레매틱스 예시에서는 장비 데이터 수집 파이프라인부터 그 데이터가 적재되는 피처 테이블, 예지보전 AI가 읽는 입력까지가 1순위다.

3.2 관측 우선순위 판단 기준
| 후보 흐름 | 업무 영향도 | 변동·고장 빈도 | 착수 순위 |

-->

| 후보 흐름 | 업무 영향도 | 변동·고장 빈도 | 적용 순위 |

관측 대상 우선순위 사분면 표

-->

삭제

영향도가 높고 변동도 잦은 오른쪽 위(텔레매틱스 흐름)는 관측 수준을 가장 높게 적용하고, 영향도는 높지만 변동이 적은 흐름은 기본 관측을 빠짐없이 적용한다. 영향도가 낮은 정적 데이터는 후순위로 둔다.

-->

영향도가 높고 변동도 잦은 텔레매틱스 흐름은 관측 수준을 가장 높게 적용하고, 영향도는 높지만 변동이 적은 흐름은 기본 관측을 빠짐없이 적용한다. 영향도가 낮은 정적 데이터는 후순위로 둔다.

4. How — 어떻게 준비·운영하나
관측은 한 번 켜고 끝나는 것이 아니라, 대상을 연결하고 정상 기준을 잡아 알림을 걸고, 운영하며 알림 피로를 줄여 나가는 과정이다. 본 장은 적용 전후의 차이부터, 구축 절차, 지표·판단 기준 설정, 알림·에스컬레이션 운영, 운영 성과 측정까지 순서대로 다룬다.

-->

관측은 한 번 설정하고 종료되는 것이 아니라, 대상을 연결하고 정상 기준을 정의한 뒤 알림을 설정하여, 운영하며 알림 피로를 줄여 나가는 과정이다. 본 장은 적용 전후의 차이부터, 구축 절차, 지표·판단 기준 설정, 알림·에스컬레이션 운영, 운영 성과 측정까지 순서대로 다룬다.

4.1 적용 전후 비교
| 구분 | 관측 없을 때 | 관측 있을 때 |

-->

| 구분 | 적용 전 | 적용 후 |

(다이어그램) 관측 없을 때, 관측 있을 때

-->

(다이어그램) 적용 전, 적용 후

4.2 Observability 구축 절차
③ 알림 규칙 설정. 4축마다 지표를 걸고, 이상 시 심각도 등급·알림 대상·경로를 2.4의 표대로 지정한다.

-->

③ 알림 규칙 설정. 4축마다 지표를 설정하고, 이상 시 심각도 등급·알림 대상·경로를 2.4의 표대로 지정한다.

4.5 운영 성과 측정
관측 체계의 운영 수준은 "얼마나 빨리 잡고, 얼마나 안 놓치며, 얼마나 넓게 보는가"로 잰다. 아래 다섯 지표를 핵심으로 둔다. 절대 기준값보다 자체 목표(SLA)를 먼저 세우고 그와 비교하는 방식을 권장한다.

-->

관측 체계의 운영 수준은 "얼마나 빨리 잡고, 얼마나 안 놓치며, 얼마나 넓게 보는가"로 측정한다. 아래 다섯 지표를 핵심으로 둔다. 절대 기준값보다 자체 목표(SLA)를 먼저 세우고 그와 비교하는 방식을 권장한다.

이상 감지 시간과 놓친 건수는 1.2에서 말한 "이상을 사전에 감지한다"는 목표와 직접 이어진다. 오탐률은 4.4의 알림 피로와 맞물린다. 오탐률을 무리하게 0으로 누르면 놓친 건수가 늘 수 있으므로, 두 지표는 함께 본다. 커버리지는 3장의 우선순위에 따라 핵심 흐름부터 100%에 가깝게 올리는 것을 목표로 한다.

-->

이상 감지 시간과 놓친 건수는 1.2에서 말한 "이상을 사전에 감지한다"는 목표와 직접 이어진다. 오탐률은 4.4의 알림 피로와 맞물린다. 오탐률을 무리하게 0으로 누르면 놓친 건수가 늘 수 있으므로, 두 지표는 함께 봐야한다. 커버리지는 3장의 우선순위에 따라 핵심 흐름부터 100%에 가깝게 올리는 것을 목표로 한다.
