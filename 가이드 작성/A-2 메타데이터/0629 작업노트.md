2.1 메타데이터란 + 체계 내 위치
메타데이터는 "데이터에 대한 데이터" — 데이터가 무엇을 뜻하고, 어떤 단위·기준으로 쌓였으며, 어떻게 써야 하는지를 적은 설명서다.

-->

메타데이터는 "데이터에 대한 데이터"로, 데이터가 무엇을 뜻하고, 어떤 단위·기준으로 쌓였으며, 어떻게 써야 하는지를 적은 설명서다.

데이터 값 자체는 그것만 봐서는 의미가 불완전하다. 예를 들어 어떤 셀에 94.2라는 숫자가 들어 있다고 하자. 이것이 동박 두께인지 수율인지, 단위가 ㎛인지 %인지, 언제 측정된 값인지, 결측이면 무엇으로 채우는지 — 값만으로는 알 수 없다. 메타데이터는 그 값 옆에 붙는 설명이다.

-->

데이터 값 자체만 봐서는 완전한 의미를 알 수 없다. 예를 들어 어떤 셀에 94.2라는 숫자가 들어 있다고 했을 때, 이것이 동박 두께인지 수율인지, 단위가 ㎛인지 %인지, 언제 측정된 값인지, 결측이면 무엇으로 채우는지 등을 값만으로는 알 수 없다. 이를 해결하기 위해 그 값 옆에 붙는 설명인 메타데이터가 필요하다.

메타데이터는 "찾을 수 있게(Findable)" 묶음(A-1\~A-3)에서 자산의 속성을 담당한다 — 용어의 뜻(A-3)과 자산의 소재(A-1) 사이에서, 자산 안의 구조·단위·의미를 채운다.

-->

메타데이터는 AI-ready Data 체계에서 자산의 속성을 담당하며, A-3 비즈니스 Glossary와 A-1 데이터 카탈로그 사이에서, 자산 안의 구조·단위·의미를 채운다.

메타데이터를 "세 종류로 무엇을 설명하나"(열) × "다섯 계층 중 어디에 붙나"(행)의 격자로 보면, 어느 칸을 누가 채울지가 한눈에 정리된다 — 아래 표의 한 칸 한 칸이 채울 대상이다.

-->

메타데이터를 "세 종류로 무엇을 설명하나"(열) × "다섯 계층 중 어디에 붙나"(행)의 격자로 보면, 어느 칸을 누가 채울지가 한눈에 정리할 수 있다.

2.2 세 종류의 메타데이터 (기술 · 비즈 · 운영)
메타데이터는 시스템 관점(기술), 업무 관점(비즈), 상태 관점(운영) 세 종류로 나뉜다 — 채우는 주체와 방식이 다르므로, 종류를 가르는 것이 작성 전략의 출발점이다.

-->

메타데이터는 시스템 관점(기술), 업무 관점(비즈), 상태 관점(운영) 세 종류로 나뉜다 — 채우는 주체와 방식이 다르므로, 종류를 구분하는 것이 작성 전략의 출발점이다.

데이터의 저장 구조·형식·생성 방식 등 시스템 관점의 속성이다. 컬럼 타입, NULL 허용 여부, 컬럼 순서, 저장 위치, 생성/변경 시각 등. 시스템에 의해 자동 생성·갱신되며 별도 수기 작성이 필요 없다 — 운영 관점에서는 "작성"이 아니라 "자동 수집이 정상으로 들어오는지" 를 관리한다.

-->

데이터의 저장 구조·형식·생성 방식 등 시스템 관점의 속성이다. 컬럼 타입, NULL 허용 여부, 컬럼 순서, 저장 위치, 생성/변경 시각 등. 시스템에 의해 자동 생성·갱신되며 별도 수기 작성이 필요 없다. 운영 관점에서는 "작성"이 아니라 "자동 수집이 정상으로 들어오는지" 를 관리한다.

데이터가 비즈니스적으로 무엇을 의미하고 어떤 업무 맥락에서 쓰이는지를 설명한다. 컬럼 의미 설명(Comment), 단위, 집계 기준, 관련 도메인, 담당 부서 등. 업무 의미를 가장 잘 아는 현업 담당자가 직접 작성한다 — 자동화가 가장 어렵고, 그래서 메타데이터 작성 부담이 여기에 몰린다(§5.2에서 작성법을 깊게 다룬다).

-->

데이터가 비즈니스적으로 무엇을 의미하고 어떤 업무 맥락에서 쓰이는지를 설명한다. 컬럼 의미 설명(Comment), 단위, 집계 기준, 관련 도메인, 담당 부서 등. 업무 의미를 가장 잘 아는 현업 담당자가 직접 작성한다. 그렇기 때문에 자동화가 가장 어렵고, 메타데이터 작성 부담이 여기에 몰린다(§5.2에서 작성법을 깊게 다룬다).

2.4 항목 사전 — 대표 항목 (현업 실행 키트 ㉠)
이 표 보는 법: 자동 항목은 플랫폼이 자동으로 채우니 사람은 확인만 한다. 사람이 실제로 손대는 건 오너·보안 칸뿐이다 — 그래서 "메타데이터 작성"의 부담은 비즈 메타에 몰려 있다. 본문은 현업이 먼저 보는 대표 항목만, 컬럼 단위 30여 항목 전체는 [Appendix A]에.

-->

이 표 보는 법: 자동 항목은 플랫폼이 자동으로 채우므로 사람은 확인만 수행하며, 실제로 사람이 직접 입력하는 항목은 오너와 보안 칸에 한정된다. 따라서 메타데이터 작성의 부담은 주로 비즈 메타에 집중된다. 본문은 현업이 먼저 보는 대표 항목만, 컬럼 단위 30여 항목 전체는 [Appendix A]에.

2.5 필드 설명서 — 이름만으론 모르는 데이터 풀이
약어·코드·현장식 이름에는 자연어 설명(Comment) 과 코드값 풀이(Dictionary) 를 붙인다 — AI와 사람이 똑같이 해석하도록.

-->

AI와 사람이 똑같이 해석하도록 약어·코드·현장식 이름에는 자연어 컬럼 설명(Comment) 과 코드값 풀이(Dictionary) 를 붙인다.

ii) 코드값 풀이(Dictionary) — 코드 컬럼은 값마다 뜻을 단다. 코드만 있고 풀이가 없으면 AI는 코드를 "의미 없는 문자열"로 본다.

-->

ii) 코드값 풀이(Dictionary) — 코드만 있고 풀이가 없으면 AI는 코드를 "의미 없는 문자열"로 보기 때문에 코드 컬럼에는 값마다 풀이를 등록한다.

iii) 좋은 예 vs 나쁜 예 (컬럼 설명)

-->

iii) 우수 예시 vs 미흡 예시 (컬럼 설명)

| Before (나쁜 예) | After (좋은 예) |

-->

| Before (미흡 예시) | After (우수 예시) |

| 항목 | 무엇을 | 예시 |

-->

| 항목 | 설명 | 예시 |

컬럼 하나하나는 설명해도, 그 테이블을 어떤 업무 규칙으로 읽어야 하는지·어떤 함정이 있는지는 따로 적어야 한다. 두 가지를 둔다.

-->

컬럼 단위의 설명만으로는 해당 테이블을 어떤 업무 규칙으로 해석해야 하는지와 어떤 함정이 있는지를 충분히 전달하기 어려우므로, 이를 별도로 기록하기 위해 두 가지 항목을 둔다.

이 두 항목이 없으면 AI와 사용자가 같은 테이블을 보고도 다른 숫자를 답한다 — "왜 대시보드와 다르지?"의 대부분이 여기서 갈린다.

-->

이 두 항목이 없으면 AI와 사용자가 동일한 테이블을 보고도 서로 다른 숫자를 산출할 수 있으며, “왜 대시보드와 다르지?”라는 문제의 상당수가 여기에서 발생한다.

2.6 단위·기준일·집계 기준
숫자 컬럼에는 단위·기준 시점·집계 방식·기준 시스템을 메타데이터로 못박아, AI가 단위·기준이 다른 값을 잘못 비교하지 않게 한다.

-->

숫자 컬럼에는 단위·기준 시점·집계 방식·기준 시스템을 메타데이터로 명확히 정의해, AI가 단위·기준이 다른 값을 잘못 비교하지 않게 한다.

| 무엇을 | 왜 필요 | 예시 |

-->

| 항목 | 필요한 이유 | 예시 |

2.7 메타데이터 스키마(메타-메타데이터)와 버전 관리
"어떤 항목을 어떤 형식으로 채울지"를 정한 표준 스키마가 메타-메타데이터다 — 이걸 고정해야 자산마다 제멋대로 채워지지 않는다.

-->

메타-메타데이터는 "어떤 항목을 어떤 형식으로 채울지"를 정한 표준 스키마로, 메타-메타데이터의 고정을 통해 자산마다 제각각 입력되는 것을 방지할 수 있다.

용어 풀이 — 메타-메타데이터: "메타데이터에 대한 메타데이터". 즉 메타데이터를 적는 양식 그 자체. 예: "컬럼 설명은 필수, 한 문장, 표준 용어 사용"이라는 규칙이 메타-메타데이터다.

-->

내용 중복되므로 삭제

참고 — 정립된 표준: 메타데이터를 표준 레지스트리로 관리하는 개념은 ISO/IEC 11179[9], AI 학습용(ML-ready) 데이터셋 메타데이터 형식은 Croissant(MLCommons)[8], 공공 데이터 메타데이터 교환은 DCAT[10] 등으로 정립돼 있다 — 우리 표준 스키마도 이들을 참고한다.

-->

참고 — 정립된 표준: 메타데이터를 표준 레지스트리로 관리하는 개념은 ISO/IEC 11179[9], AI 학습용(ML-ready) 데이터셋 메타데이터 형식은 Croissant(MLCommons)[8], 공공 데이터 메타데이터 교환은 DCAT[10] 등으로 정립돼 있다. 본 가이드의 표준 스키마는 이들을 참고했음.

표준 스키마 정의: 종류별(기술/비즈/운영)·계층별로 필수/선택 항목, 형식, 허용값을 표로 못박는다([Appendix A]가 그 예). 이 표가 곧 "모든 자산이 따르는 양식"이 된다.

-->

표준 스키마 정의: 종류별(기술/비즈/운영)·계층별로 필수/선택 항목, 형식, 허용값을 표로 정의한다(예: [Appendix A]). 이 표가 곧 "모든 자산이 따르는 양식"이 된다.

3. What — 데이터 유형별 메타데이터 차이
정형·시계열·문서·이미지·영상은 챙겨야 할 메타데이터 항목이 다르다 — 유형을 먼저 가르고, 그 유형에 맞는 항목을 채운다.

-->

정형 데이터, 시계열 데이터, 문서, 이미지, 영상은 각각 필요한 메타데이터 항목이 다르므로, 먼저 유형을 구분한 뒤 해당 유형에 맞는 항목을 입력한다.

메타데이터를 "모든 데이터에 같은 항목"으로 채우려 하면, 시계열엔 불필요한 항목을 묻고 정작 중요한 측정 주기는 빠진다. 그래서 유형부터 가른다.

-->

메타데이터를 모든 데이터에 동일한 항목으로 일괄 적용하면 시계열 데이터에는 불필요한 항목이 포함되고 정작 중요한 측정 주기가 누락될 수 있으므로, 먼저 데이터 유형을 구분한다.

4.1 정비 우선 대상
전 자산을 한 번에 하지 않는다 — AI 과제가 당장 쓸 데이터, 설명이 비었거나 단위가 뒤죽박죽인 데이터부터. 메타데이터는 기본적으로 다 필요하지만, 채우는 순서를 정한다.

-->

메타데이터는 기본적으로 다 필요하지만, 채우는 순서를 정해 전 자산을 한 번에 하는 것이 아닌, AI 과제가 당장 쓸 데이터, 설명이 비었거나 단위가 뒤죽박죽인 데이터부터 정비한다.

두산전자 예시: 동박 결함 예측 과제가 쓰는 INSP_RESULT·PROD_LOG의 컬럼부터 비즈 메타를 채우고, 잘 안 쓰는 레거시 백업 테이블은 뒤로 미룬다. "전부 다"가 아니라 "쓰는 것부터".

-->

두산전자 예시: 동박 결함 예측 과제가 쓰는 INSP_RESULT·PROD_LOG의 컬럼부터 비즈 메타를 채우고, 잘 안 쓰는 레거시 백업 테이블은 뒤로 미룬다.

4.2 자동으로 모으고, 사람은 검수만
기술·운영·보안 메타는 시스템이 자동으로 채우고, 사람은 비즈·AI 메타(의미·단위·집계)만 보완한다 — "다 적는다"가 아니라 "빈 칸만 메운다".

-->

기술·운영·보안 메타데이터는 시스템이 자동으로 채우고, 사람은 비즈니스·AI 메타데이터의 의미·단위·집계 관련 항목만 보완하므로, “모두 작성한다”가 아니라 “빈 칸만 보완한다”는 원칙으로 운영한다.

이 분담이 메타데이터 운영의 핵심이다. 만약 모든 항목을 사람이 적게 하면 작성은 시작도 못 하고 멈춘다. 자동으로 채울 수 있는 80%(기술·운영)는 플랫폼에 맡기고, 사람은 자동화가 어려운 20%(의미·단위·집계)에만 집중한다.

-->

이 분담은 메타데이터 운영의 핵심이며, 모든 항목을 사람이 직접 작성하면 운영이 시작부터 정체될 수 있다. 따라서 자동으로 채울 수 있는 80%(기술·운영 메타데이터)는 플랫폼이 담당하고, 사람은 자동화가 어려운 20%(의미·단위·집계)에 집중한다.

5. How — 어떻게 준비·운영하나
이 장은 메타데이터를 실제로 어떻게 수집·작성·검수하고 운영하는지를 다룬다. 자동 수집을 연결한 뒤 우선 대상부터 비즈 메타를 채우고(5.15.3), 정합성을 점검해 등록하며 갱신·역할을 정한다(5.45.5). 어떤 솔루션으로 이 일을 할지는 6장 Tech Stack에서 비교한다.

-->

"정합성을 점검해 등록하며 갱신·역할을 정한다(5.45.5)" 부분에 취소선 그어져있음.

5.2 비즈 메타 작성 — 좋은 예 vs 나쁜 예 (현업 실행 키트 ㉡)
-->

5.2 비즈 메타 작성 — 우수 예시 vs 미흡 예시 (현업 실행 키트 ㉡)
비즈 메타는 무엇을·어떤 기준으로 집계했는지가 드러나게, 핵심 키워드 중심으로 1\~2문장 안에 쓴다 — 아래 교정 예를 그대로 따라 쓴다.

-->

비즈 메타는 무엇을·어떤 기준으로 집계했는지가 드러나도록, 핵심 키워드 중심으로 1\~2문장 내로 작성한다.

무엇을·어떤 기준으로 집계했는지 드러나게 — 명확한 핵심 정보 중심.

핵심 키워드 중심, 1\~2문장 이내 — 길게 서술하지 않는다.

모호어 금지 — 해석이 갈리는 정성 표현 대신 정량 기준.

-->

명확한 핵심 정보 중심으로 무엇을·어떤 기준으로 집계했는지 드러나도록 작성한다.

핵심 키워드 중심으로, 1\~2문장 이내로 작성한다.

해석이 갈리는 정성 표현(모호어) 대신 정량 기준으로 작성한다.

| 나쁜 예 (Before) | 좋은 예 (After) | 교정 사유 |

-->

| 미흡 예시 (Before) | 우수 예시 (After) | 교정 사유 |

금지 표현: 일부 · 대략 · 주요 · 관련 · 상세 · 최근 — 해석이 사람마다 갈린다. 측정 기준이 분명한 정량 표현으로 바꾼다.

-->

금지 표현: 일부 · 대략 · 주요 · 관련 · 상세 · 최근 등은 해석이 사람마다 갈리므로, 측정 기준이 분명한 정량 표현으로 바꾼다.

5.3 실제로 어디서 채우나 — 플랫폼 매핑 (현업 실행 키트 ㉤)
기술·운영 메타는 플랫폼이 이미 보관하는 시스템 메타데이터에서 끌어온다 — 사람이 새로 적는 게 아니다.

-->

기술·운영 메타는 사람이 새로 작성하는 것이 아닌, 플랫폼이 이미 보관하는 시스템 메타데이터에서 끌어온다.

5.4 정합성 자동 점검
| 점검 규칙 | 잡아내는 것 | 조치 |

-->

| 점검 항목 | 상세 | 조치 |

| 점검 | 신호 | 조치 |

-->

| 점검 항목 | 상세 | 조치 |

5.5 운영 — 갱신·승인과 역할
기술 메타는 구조 변경 시 자동 반영되지만, 비즈 메타는 자동으로 안 바뀐다 — 그래서 구조 변경 시 "의미 재확인" 알림이 오너에게 가야 한다. 변경은 기존 정의를 덮어쓰지 않고 변경 담당자·변경일·변경 사유와 함께 이력으로 남긴다.

-->

기술 메타는 구조 변경 시 자동 반영되지만, 비즈 메타는 자동으로 안 바뀌므로, 구조 변경 시 "의미 재확인" 알림이 오너에게 가야 한다. 변경은 기존 정의를 덮어쓰지 않고 변경 담당자·변경일·변경 사유와 함께 이력으로 남긴다.

역할별 책임 (RACI 요약):

-->

역할별 책임:

RACI 표

-->

삭제

용어 풀이 — RACI: R(실행)·A(최종책임)·C(협의)·I(통보). "누가 실제로 하고, 누가 최종 승인하나"를 활동별로 못박는 표.

-->

삭제

6.2 선정 기준
솔루션을 고를 때는 메타데이터 관점에서 다음 여섯 축을 가중 평가한다 — 핵심은 "자동 수집이 얼마나 되고, 사람이 비즈 메타를 실제로 쉽게 쓸 수 있는가"다.

-->

솔루션을 고를 때는 메타데이터 관점에서 다음 여섯 축을 가중 평가하며, 그중 핵심은 "자동 수집이 얼마나 되고, 사람이 비즈 메타를 실제로 쉽게 쓸 수 있는가"다.
