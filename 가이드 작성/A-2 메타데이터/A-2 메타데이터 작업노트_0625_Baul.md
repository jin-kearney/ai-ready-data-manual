전체
Glossary --> 비즈니스 Glossary

카탈로그 --> 데이터 카탈로그

기술·비즈·운영 메타 --> 기술·비즈·운영 메타데이터

★ 삭제

1.1 메타데이터란 (데이터의 설명서) --> 메타데이터란
메타데이터는 "데이터에 대한 데이터" — 데이터가 무엇을 뜻하고, 어떤 단위·기준으로 쌓였으며, 어떻게 써야 하는지를 적은 설명서다.

-->

메타데이터는 "데이터에 대한 데이터"로, 데이터가 무엇을 뜻하고, 어떤 단위·기준으로 쌓였으며, 어떻게 써야 하는지를 적은 설명서다.

데이터 값 자체는 그것만 봐서는 의미가 불완전하다. 예를 들어 어떤 셀에 94.2라는 숫자가 들어 있다고 하자. 이것이 동박 두께인지 수율인지, 단위가 ㎛인지 %인지, 언제 측정된 값인지, 결측이면 무엇으로 채우는지 — 값만으로는 알 수 없다. 메타데이터는 그 값 옆에 붙는 설명이다.

-->

데이터 값 자체만 봐서는 완전한 의미를 알 수 없다. 예를 들어 어떤 셀에 94.2라는 숫자가 들어 있다고 했을 때, 이것이 동박 두께인지 수율인지, 단위가 ㎛인지 %인지, 언제 측정된 값인지, 결측이면 무엇으로 채우는지 등을 값만으로는 알 수 없다. 이를 해결하기 위해 그 값 옆에 붙는 설명인 메타데이터가 필요하다.

두산전자 예시: 컬럼명 DEF_CD 하나만 보면 AI는 이게 무슨 코드인지 모른다. 메타데이터로 "품질 결함 유형 코드 (예: S01=스크래치, P03=핀홀)" 를 붙이면, AI가 "스크래치 불량 추이 보여줘"라는 질문에 이 컬럼을 정확히 연결한다. 설명서 한 줄이 데이터를 "사람만 아는 자산"에서 "AI도 쓰는 자산"으로 바꾼다.

-->

두산전자 예시: AI는 컬럼명 DEF_CD 하나만 보고 무슨 코드인지 파악할 수 없다. 하지만 칼럼에 메타데이터로 "품질 결함 유형 코드 (예: S01=스크래치, P03=핀홀)" 를 붙이면, AI가 "스크래치 불량 추이 보여줘"라는 질문에 이 컬럼을 정확히 연결한다. 메타데이터는 데이터를 "사람만 아는 자산"에서 "AI도 쓰는 자산"으로 바꾸는 역할을 한다.

1.2 적용 범위와 체계 내 위치 (자산의 "속성") --> 적용 범위와 AI-ready Data 체계 내 위치
메타데이터는 "찾을 수 있게(Findable)" 묶음(A-1\~A-3)에서 자산의 속성을 담당한다 — 용어의 뜻(A-3)과 자산의 소재(A-1) 사이에서, 자산 안의 구조·단위·의미를 채운다.

-->

메타데이터는 AI-ready Data 체계에서 자산의 속성을 담당하며, A-3 비즈니스 Glossary와 A-1 데이터 카탈로그 사이에서, 자산 안의 구조·단위·의미를 채운다.

A-1\~A-3은 "AI와 사람이 데이터를 찾을 수 있게" 만드는 세 주제다. 각자 다른 질문에 답한다.

-->

A-1\~A-3은 "AI와 사람이 데이터를 찾을 수 있게" 만드는 세 주제로, 각자 다른 질문에 답한다.

| 주제 | 답하는 질문 | 무엇을 담나 | 비유 |

-->

| 주제 | 답하는 질문 | 포함 항목 | 비유 |

| 다룬다 (A-2) | 안 다룬다 (다른 주제) |

-->

| 다루는 것 (A-2) | 안 다루는 것 (다른 주제) |

1.3 주요 대상 조직
메타데이터는 "한 조직이 다 만드는 것"이 아니라, 자동으로 채워지는 부분과 사람이 채우는 부분이 나뉜다. 누가 무엇을 하는지가 명확해야 작성 부담이 엉뚱한 곳에 쏠리지 않는다.

-->

메타데이터는 "한 조직이 다 만드는 것"이 아니라, 자동으로 채워지는 부분과 사람이 채우는 부분이 나뉜다. 누가 무엇을 하는지를 명확하게 구분해 작성 부담이 부적절한 곳에 쏠리지 않도록 방지한다.

2. 왜 필요한가 (Why)
이름·단위만으론 뜻을 알 수 없어 AI가 잘못 해석하고, 같은 숫자를 부서마다 다른 기준으로 비교해 틀린 답이 나온다 — 메타데이터는 이 오해석을 원천에서 막는다.

-->

이름·단위만으론 뜻을 알 수 없어 AI가 잘못 해석하고, 같은 숫자를 부서마다 다른 기준으로 비교해 틀린 답이 나오므로, 메타데이터는 오해석을 원천에서 방지한다.

2.1 현업 Pain Point
A-1 카탈로그가 "데이터를 찾는" 문제를 푼다면, 메타데이터는 그다음 문제 — "찾았는데 무슨 뜻인지 모른다"를 푼다. 두산전자 AI 과제팀이 동박 결함 예측 모델을 위해 검사결과 테이블 INSP_RESULT를 찾았다고 하자. 카탈로그 덕분에 위치·오너는 알았다. 그런데 테이블을 열어 보니:

-->

A-1 데이터 카탈로그가 "데이터를 찾는" 문제를 푼다면, 메타데이터는 그다음 문제인 "찾았는데 무슨 뜻인지 모른다"를 푼다. 예를 들어, 두산전자 AI 과제팀이 동박 결함 예측 모델을 위해 검사결과 테이블 INSP_RESULT를 찾았고, 데이터 카탈로그를 통해 위치·오너는 파악할 수 있었지만, 테이블을 열어보니 다음과 같은 문제점이 있었다.

컬럼이 DEF_CD, WIP_QTY, THK처럼 현장식 약어다. 이게 결함 코드인지, 재공 수량인지, 두께인지 — 만든 사람 외에는 모른다. AI는 더더욱 모른다. 결국 "이 컬럼 뭐예요?"를 사람에게 물어봐야 하고, 그 사람이 자리를 비우면 과제가 멈춘다.

-->

컬럼이 DEF_CD, WIP_QTY, THK처럼 현장식 약어일 때, 이것이 결함 코드인지, 재공 수량인지, 두께인지 등은 만든 사람 외에는 AI도 알 수 없다. 결국 이 컬럼이 무엇인지를 사람에게 물어봐야 하고, 그 사람이 자리를 비우면 과제가 멈춘다.

2.2 기대 효과
두산전자 예시: 신규 분석 담당자가 INSP_RESULT를 처음 봐도, 컬럼 설명·코드값 풀이만 읽고 바로 결함 분석을 시작한다 — 기존엔 선임에게 컬럼 의미를 묻느라 며칠이 걸렸다.

-->

신규 분석 담당자가 INSP_RESULT를 처음 봐도, 기존엔 선임에게 컬럼 의미를 묻느라 며칠이 걸렸던 것을 컬럼 설명·코드값 풀이만 읽고 바로 결함 분석을 시작할 수 있다.

3. 무엇을 갖추나 (What — 세 종류·다섯 계층)
★ 이 절의 정본 모델: 메타데이터 = 세 종류(기술·비즈·운영) × 다섯 계층(카탈로그·스키마·테이블·컬럼·볼륨). 가이드 전체에서 이 분류·용어를 일관되게 쓴다.

-->

이 절의 정본 모델: 메타데이터 = 세 종류(기술·비즈·운영) × 다섯 계층(카탈로그·스키마·테이블·컬럼·볼륨). 가이드 전체에서 이 분류·용어를 일관되게 쓴다.

메타데이터를 "세 종류로 무엇을 설명하나"(열) × "다섯 계층 중 어디에 붙나"(행)의 격자로 보면, 어느 칸을 누가 채울지가 한눈에 정리된다 — 아래 표의 한 칸 한 칸이 채울 대상이다.

-->

메타데이터를 "세 종류로 무엇을 설명하나"(열) × "다섯 계층 중 어디에 붙나"(행)의 격자로 보면, 어느 칸을 누가 채울지가 한눈에 정리할 수 있다.

| ④ 컬럼 **(가장 촘촘)** | 타입·NULL·길이 | **의미·단위·코드값 풀이** | 품질 점수·결측률 |

-->

| ④ 컬럼 | 타입·NULL·길이 | **의미·단위·코드값 풀이** | 품질 점수·결측률 |

읽는법: 자동으로 채워지는 칸(기술·운영)은 플랫폼이 자동으로 채우고, 사람이 실제로 손대는 건 비즈 열, 특히 ④ 컬럼 × 비즈 칸(의미·단위·코드값)이다 — 메타데이터 작성의 무게가 어느 한 칸에 몰려 있는지를 격자가 보여준다. 종류는 §3.1, 계층은 §3.2에서 각각 자세히 본다.

-->

읽는법: 자동으로 채워지는 칸(기술·운영)은 플랫폼이 자동으로 채우고, 사람이 실제로 손대는 건 비즈 열, 특히 ④ 컬럼 × 비즈 칸(의미·단위·코드값)이다. 종류는 §3.1, 계층은 §3.2에서 각각 자세히 볼 수 있다.

3.1 세 종류의 메타데이터 (기술 · 비즈 · 운영)
메타데이터는 시스템 관점(기술), 업무 관점(비즈), 상태 관점(운영) 세 종류로 나뉜다 — 채우는 주체와 방식이 다르므로 종류를 가르는 것이 작성 전략의 출발점이다.

-->

메타데이터는 채우는 주체와 방식이 다르므로 시스템 관점(기술), 업무 관점(비즈), 상태 관점(운영) 세 종류로 나눌 수 있다.

데이터의 저장 구조·형식·생성 방식 등 시스템 관점의 속성이다. 컬럼 타입, NULL 허용 여부, 컬럼 순서, 저장 위치, 생성/변경 시각 등. 시스템에 의해 자동 생성·갱신되며 별도 수기 작성이 필요 없다 — 운영 관점에서는 "작성"이 아니라 "자동 수집이 정상으로 들어오는지" 를 관리한다.

-->

데이터의 저장 구조·형식·생성 방식 등 시스템 관점의 속성이다. 컬럼 타입, NULL 허용 여부, 컬럼 순서, 저장 위치, 생성/변경 시각 등. 시스템에 의해 자동 생성·갱신되며 별도 수기 작성이 필요 없다. 운영 관점에서는 "작성"이 아니라 "자동 수집이 정상으로 들어오는지" 를 관리한다.

데이터가 비즈니스적으로 무엇을 의미하고 어떤 업무 맥락에서 쓰이는지를 설명한다. 컬럼 의미 설명(Comment), 단위, 집계 기준, 관련 도메인, 담당 부서 등. 업무 의미를 가장 잘 아는 현업 담당자가 직접 작성한다 — 자동화가 가장 어렵고, 그래서 메타데이터 작성 부담이 여기에 몰린다(§7.3에서 작성법을 깊게 다룬다).

-->

데이터가 비즈니스적으로 무엇을 의미하고 어떤 업무 맥락에서 쓰이는지를 설명한다. 컬럼 의미 설명(Comment), 단위, 집계 기준, 관련 도메인, 담당 부서 등. 업무 의미를 가장 잘 아는 현업 담당자가 직접 작성한다. 자동화가 가장 어려운 부분에 해당하므로, 메타데이터 작성 부담이 여기에 몰린다(§7.3에서 작성법을 깊게 다룬다).

용어 풀이 — "AI 메타"(이 가이드에서): 별도의 네 번째 종류가 아니라 비즈 메타의 한 갈래다 — AI가 자산을 더 잘 쓰도록 보태는 항목(예: 문서 요약 content_summary, 언급 대상 entities, "AI 활용 가능 등급")으로, 사람이 정하거나 AI가 초안을 만들고 사람이 검수한다. 뒤에서 "비즈·AI 메타"라고 묶어 부르는 것은 둘 다 사람이 보완하는 칸이라는 뜻이지, 종류가 늘어난 게 아니다(정본은 끝까지 세 종류).

-->

문맥상 맞지 않으므로 삭제

| 종류 | 무엇을 설명 | 대표 항목 | 누가·어떻게 |

-->

| 종류 | 설명 대상 | 대표 항목 | 작성 주체·방법 |

컬럼 의미(Comment), 단위, 집계 대상·기간·포함조건, 계산 로직, 기준 시스템, 관련 도메인, 활용 시나리오, 담당 부서, 표준 용어 링크(Glossary)

-->

컬럼 의미, 단위, 집계 대상·기간·포함 조건, 계산 로직, 기준 시스템, 관련 도메인, 활용 시나리오, 담당 부서, 표준 용어 링크(비즈니스 Glossary)

3.2 다섯 계층 (어디에 붙나)
메타데이터는 데이터 자산의 계층마다 붙는다 — 위에서 아래로 좁아지며, 컬럼이 가장 촘촘하고, 표가 아닌 자산(문서·이미지)은 볼륨 단위로 관리한다.

-->

메타데이터는 데이터 자산의 계층마다 붙어 위에서 아래로 좁아지며, 컬럼이 가장 촘촘하고, 표가 아닌 자산(문서·이미지)은 볼륨 단위로 관리한다.

| 계층 | 이 계층 메타의 초점 | 대표 항목 |

-->

| 계층 | 메타데이터 초점 | 대표 항목 |

| ④ 컬럼 | **가장 촘촘** — 의미·타입·단위·코드 | 컬럼명, 타입, NULL, 의미(Comment), 단위, 코드값 |

-->

| ④ 컬럼 | 의미·타입·단위·코드 | 컬럼명, 타입, NULL, 칼럼 의미, 단위, 코드값 |

3.3 항목 사전 — 대표 항목 (현업 실행 키트 ㉠)
자산에 붙이는 메타데이터 항목을 세 종류로 묶어 정리한 표다. 필수/선택은 최소 채울 항목, 작성 주체는 자동(플랫폼) / 오너(현업) / 보안으로 나뉜다.

-->

자산에 붙이는 메타데이터 항목을 기술·비즈·운영 메타데이터로 묶어 정리한 표. 필수/선택은 최소 채울 항목, 작성 주체는 자동(플랫폼) / 오너(현업) / 보안으로 나뉜다.

이 표 보는 법: 자동 항목은 플랫폼이 자동으로 채우니 사람은 확인만 한다. 사람이 실제로 손대는 건 오너·보안 칸뿐이다 — 그래서 "메타데이터 작성"의 부담은 비즈 메타에 몰려 있다. 본문은 현업이 먼저 보는 대표 항목만, 컬럼 단위 30여 항목 전체는 [Appendix A]에.

-->

이 표 보는 법: 자동 항목은 플랫폼이 자동으로 채우니 사람은 확인만 하면 되고, 사람이 실제로 손대는 건 오너·보안 칸뿐이므로 "메타데이터 작성"의 부담은 비즈 메타에 몰려 있다. 본문은 현업이 먼저 보는 대표 항목만, 컬럼 단위 30여 항목 전체는 [Appendix A]에.

기술 메타데이터 (대표)

-->

기술 메타데이터 (대표 항목)

| 생성/변경 시각 | 언제 만들어/바뀌었나 | 2025-01-20 14:22 | 선택 | 자동 |

-->

| 생성/변경 시각 | 언제 만들었나/바뀌었나 | 2025-01-20 14:22 | 선택 | 자동 |

비즈 메타데이터 (대표 — 사람이 채우는 핵심)

-->

비즈 메타데이터 (대표 항목)

| 컬럼 의미 설명(Comment) | 이 컬럼이 무엇인지 한 문장 | 계약 단가 기준 확정 매출 (납품·세금계산서 발행 건) | 필수 | 오너 |

-->

| 컬럼 의미 | 이 컬럼이 무엇인지 한 문장 | 계약 단가 기준 확정 매출 (납품·세금계산서 발행 건) | 필수 | 오너 |

운영 메타데이터 (대표)

-->

운영 메타데이터 (대표 항목)

3.4 필드 설명서 — 이름만으론 모르는 데이터 풀이
약어·코드·현장식 이름에는 자연어 설명(Comment) 과 코드값 풀이(Dictionary) 를 붙인다 — AI와 사람이 똑같이 해석하도록.

-->

AI와 사람이 똑같이 해석하도록 약어·코드·현장식 이름에는 자연어 칼럼 설명과 코드값 풀이(Dictionary) 를 붙인다.

i) 컬럼 설명(Comment) — 컬럼이 담는 내용을 한 문장으로 적는다. 용어는 A-3 Glossary 표준 용어를 우선 사용하고, 없으면 Glossary에 먼저 등록 후 반영한다.

-->

i) 컬럼 설명 — 컬럼이 담는 내용을 한 문장으로 적는다. 용어는 A-3 비즈니스 Glossary 표준 용어를 우선 사용하고, 없으면 비즈니스 Glossary에 먼저 등록 후 반영한다.

iii) 좋은 설명 vs 나쁜 설명 (컬럼 Comment)

-->

iii) 좋은 예 vs 나쁜 예 (컬럼 설명)

컬럼 하나하나는 설명해도, 그 테이블을 어떤 업무 규칙으로 읽어야 하는지·어떤 함정이 있는지는 따로 적어야 한다. 두 가지를 둔다.

-->

컬럼 하나하나는 설명해도, 그 테이블을 어떤 업무 규칙으로 읽어야 하는지·어떤 함정이 있는지는 따로 적어야 한다.

| 항목 | 무엇을 | 예시 |

-->

| 항목 | 용도 | 예시 |

이 두 항목이 없으면 AI와 사용자가 같은 테이블을 보고도 다른 숫자를 답한다 — "왜 대시보드와 다르지?"의 대부분이 여기서 갈린다.

-->

이 두 항목이 없으면 AI와 사용자가 같은 테이블을 보고도 다른 숫자를 답하게 되며, "왜 대시보드와 다르지?"의 주요 원인이 된다.

3.5 단위·기준일·집계 기준
| 무엇을 | 왜 필요 | 예시 |

-->

| 항목 | 필요한 이유 | 예시 |

값의 "성격"을 표시하는 것도 중요하다. 더하면 되는 금액·수량과 더하면 안 되는 비율·코드를 구분하지 않으면 AI가 비율을 합산하는 실수를 한다(→ [Appendix C] value_type 태그).

-->

더하면 되는 금액·수량과 더하면 안 되는 비율·코드를 구분하지 않으면 AI가 비율을 합산하는 실수를 하므로, 값의 "성격"을 표시하는 것도 중요하다.(→ [Appendix C] value_type 태그).

3.6 메타데이터 스키마(메타-메타데이터)와 버전 관리
"어떤 항목을 어떤 형식으로 채울지"를 정한 표준 스키마가 메타-메타데이터다 — 이걸 고정해야 자산마다 제멋대로 채워지지 않는다.

-->

메타-메타데이터는 "어떤 항목을 어떤 형식으로 채울지"를 정한 표준 스키마로, 이를 고정해야 자산마다 제멋대로 채워지지 않는다.

참고 — 정립된 표준: 메타데이터를 표준 레지스트리로 관리하는 개념은 ISO/IEC 11179, AI 학습용(ML-ready) 데이터셋 메타데이터 형식은 Croissant(MLCommons), 공공 데이터 메타데이터 교환은 DCAT 등으로 정립돼 있으며, 본 표준 스키마도 이들을 참고한다.

4. 데이터 유형별 메타데이터 차이
정형·시계열·문서·이미지·영상은 챙겨야 할 메타데이터 항목이 다르다 — 유형을 먼저 가르고, 그 유형에 맞는 항목을 채운다.

-->

정형·시계열·문서·이미지·영상은 핵심 메타데이터 항목이 다르므로, 유형을 먼저 구분하고, 그 유형에 맞는 항목을 채운다.

메타데이터를 "모든 데이터에 같은 항목"으로 채우려 하면, 시계열엔 불필요한 항목을 묻고 정작 중요한 측정 주기는 빠진다. 그래서 유형부터 가른다.

-->

메타데이터를 "모든 데이터에 같은 항목"으로 채우려 하면, 시계열엔 불필요한 항목을 묻고 정작 중요한 측정 주기는 빠지므로 유형부터 구분해야 한다.

4.1 유형을 가르는 3가지 기준 --> 유형 구분 기준
| 기준 | 갈래 | 예 |

-->

| 기준 | 유형 | 예 |

4.2 유형별 챙길 메타데이터 --> 유형별 핵심 메타데이터
| 유형 | 핵심 메타데이터 (이 유형만의 것) | 두산 예시 |

-->

| 유형 | 핵심 메타데이터 | 두산 예시 |

5.1 정비 우선 대상
전 자산을 한 번에 하지 않는다 — AI 과제가 당장 쓸 데이터, 설명이 비었거나 단위가 뒤죽박죽인 데이터부터. 메타데이터는 기본적으로 다 필요하지만, 채우는 순서를 정한다.

-->

메타데이터는 기본적으로 다 필요하지만, 채우는 순서를 정해 전 자산을 한 번에 하는 것이 아닌, AI 과제가 당장 쓸 데이터, 설명이 비었거나 단위가 뒤죽박죽인 데이터부터 정비한다.

두산전자 예시: 동박 결함 예측 과제가 쓰는 INSP_RESULT·PROD_LOG의 컬럼부터 비즈 메타를 채우고, 잘 안 쓰는 레거시 백업 테이블은 뒤로 미룬다. "전부 다"가 아니라 "쓰는 것부터".

-->

두산전자 예시: 동박 결함 예측 과제가 쓰는 INSP_RESULT·PROD_LOG의 컬럼부터 비즈 메타를 채우고, 잘 안 쓰는 레거시 백업 테이블은 뒤로 미룬다.

5.2 자동으로 모으고, 사람은 검수만
기술·운영·보안 메타는 시스템이 자동으로 채우고, 사람은 비즈·AI 메타(의미·단위·집계)만 보완한다 — "다 적는다"가 아니라 "빈 칸만 메운다".

-->

기술·운영·보안 메타는 시스템이 자동으로 채우고, 사람은 빈 칸만 채우는 식으로 비즈·AI 메타(의미·단위·집계)만 보완한다.

이 분담이 메타데이터 운영의 핵심이다. 만약 모든 항목을 사람이 적게 하면 작성은 시작도 못 하고 멈춘다. 자동으로 채울 수 있는 80%(기술·운영)는 플랫폼에 맡기고, 사람은 자동화가 어려운 20%(의미·단위·집계)에만 집중한다.

-->

이 분담이 메타데이터 운영의 핵심으로, 만약 모든 항목을 사람이 적게 하면 작성은 시작도 못 하고 멈출 수 있다. 자동으로 채울 수 있는 80%(기술·운영)는 플랫폼에 맡기고, 사람은 자동화가 어려운 20%(의미·단위·집계)에만 집중한다.

7.1 수집·관리 도구 검토
2층 연결: 솔루션을 묶어서 평가·선정하려면 → Tech Stack 비교 정본. 아래는 메타데이터 관점의 기능 비교(1층)다.

-->

?

메타데이터는 카탈로그 솔루션이 함께 관리하는 경우가 많다 — 자동 수집(커넥터)·비즈 메타 작성 UI·검수 워크플로·버전 관리 기능을 본다.

-->

메타데이터는 카탈로그 솔루션이 함께 관리하는 경우가 많으므로 자동 수집(커넥터)·비즈 메타 작성 UI·검수 워크플로·버전 관리 기능을 중점적으로 본다.

기능·지원 범위·가격은 변동되므로 도입 검토 시 공식 문서·PoC로 확인한다(단정 금지).

-->

기능·지원 범위·가격은 변동되므로 도입 검토 시 공식 문서·PoC로 확인한다.

| 평가 축 | 무엇을 보나 | 왜 중요 |

-->

| 평가 축 | 항목 | 중요한 이유 |

§3.6 메타스키마 진화 지원

-->

§3.6 메타데이터 스키마 진화 지원

| 유형 | 강점 | 유의점 |

-->

| 유형 | 장점 | 단점 |

7.3 비즈 메타 작성 — 좋은 예 vs 나쁜 예 (현업 실행 키트 ㉡)
비즈 메타는 무엇을·어떤 기준으로 집계했는지가 드러나게, 핵심 키워드 중심으로 1\~2문장 안에 쓴다 — 아래 교정 예를 그대로 따라 쓴다.

-->

비즈 메타데이터는 아래 교정 예시와 같이 무엇을·어떤 기준으로 집계했는지가 드러나게, 핵심 키워드 중심으로 1\~2문장 내로 작성한다.

작성 기본 원칙 3가지:

무엇을·어떤 기준으로 집계했는지 드러나게 — 명확한 핵심 정보 중심.

핵심 키워드 중심, 1\~2문장 이내 — 길게 서술하지 않는다.

모호어 금지 — 해석이 갈리는 정성 표현 대신 정량 기준.

-->

작성 기본 원칙 3가지:

명확한 핵심 정보 중심으로 무엇을·어떤 기준으로 집계했는지 드러나도록 작성한다.

핵심 키워드 중심으로, 1\~2문장 이내로 작성한다.

해석이 갈리는 정성 표현 대신 정량 기준으로 작성한다.

| 항목 | 이렇게 쓰면 (Before) | 이렇게 (After) | 왜 |

-->

| 항목 | 나쁜 예 (Before) | 좋은 예 (After) | 교정 사유 |

금지 표현: 일부 · 대략 · 주요 · 관련 · 상세 · 최근 — 해석이 사람마다 갈린다. 측정 기준이 분명한 정량 표현으로 바꾼다.

-->

금지 표현: 일부 · 대략 · 주요 · 관련 · 상세 · 최근 등의 표현은 해석이 사람마다 상이하므로, 측정 기준이 분명한 정량 표현으로 변경한다.

유사 데이터 참조 원칙 (작성 전 필수): 새로 쓰기 전에 카탈로그에서 유사 컬럼·테이블을 먼저 검색한다(키워드 예: "매출손익", "미출고 잔량", "거래처별 단가"). 유사 비즈 메타가 있으면 같은 용어·설명 구조를 참고해 표류를 막는다. 반대로 같은 테이블 안에 이름이 비슷하지만 의미가 다른 컬럼이 있으면 산식·기준 시점·포함/제외 조건의 차이를 명확히 써서 혼동을 막는다.

-->

유사 데이터 참조 원칙 (작성 전 필수): 신규 작성 전에 데이터 카탈로그에서 유사 컬럼·테이블을 먼저 검색한다(키워드 예: "매출손익", "미출고 잔량", "거래처별 단가"). 유사 비즈 메타데이터가 있으면 같은 용어·설명 구조를 참고해 용어와 설명이 일관되게 유지되도록 한다. 반대로 같은 테이블 안에 이름이 비슷하지만 의미가 다른 컬럼이 있으면 산식·기준 시점·포함/제외 조건의 차이를 명확히 작성해 혼동을 방지한다.

비즈 메타를 빠짐없이 뽑는 법 — 인자도출 프레임워크

-->

인자도출 프레임워크를 통한 중복·누락 없는 비즈 메타데이터 도출

7.4 실제로 어디서 채우나 — 플랫폼 매핑 (현업 실행 키트 ㉤)
기술·운영 메타는 플랫폼이 이미 보관하는 시스템 메타데이터에서 끌어온다 — 사람이 새로 적는 게 아니다.

-->

기술·운영 메타는 사람이 새로 작성하는 것이 아닌, 플랫폼이 이미 보관하는 시스템 메타데이터에서 끌어온다.

7.5 정합성 자동 점검
| 점검 규칙 | 잡아내는 것 | 조치 |

-->

| 점검 규칙 | 점검 항목 | 조치 |

운영 메타가 "이 데이터를 지금 믿고 써도 되는지"의 근거가 된다 — AI가 끊긴 파이프라인의 옛 데이터를 참조하지 않도록 막는다.

-->

운영 메타는 "이 데이터를 지금 믿고 써도 되는지"의 근거가 되어, AI가 끊긴 파이프라인의 옛 데이터를 참조하지 않도록 막는다.

7.6 운영 — 갱신·승인과 역할
| 트리거 | 예 | 영향받는 메타 |

-->

| 트리거 | 예 | 영향받는 메타데이터 |

기술 메타는 구조 변경 시 자동 반영되지만, 비즈 메타는 자동으로 안 바뀐다 — 그래서 구조 변경 시 "의미 재확인" 알림이 오너에게 가야 한다. 변경은 기존 정의를 덮어쓰지 않고 변경 담당자·변경일·변경 사유와 함께 이력으로 남긴다.

-->

기술 메타는 구조 변경 시 자동 반영되지만, 비즈 메타는 자동으로 변경되지 않으므로, 구조 변경 시 "의미 재확인" 알림이 오너에게 가야 한다. 변경은 기존 정의를 덮어쓰지 않고 변경 담당자·변경일·변경 사유와 함께 이력으로 남긴다.

8. 다른 주제와의 관계
메타데이터는 자산의 속성만 담는다 — 소재(A-1)·용어 뜻(A-3)·개념 관계(B-3)·흐름(C-3)·품질 측정(C-2)은 인접 주제가 맡고, 메타데이터는 그것들과 연결된다.

-->

삭제

| 인접 주제 | 경계 (Glossary는 어디까지) |

-->

| 인접 주제 | A-2와의 경계 |

9.1 성과 지표 (KPI)
메타데이터는 "얼마나 채워졌나(완성률)·얼마나 자동인가(자동 수집률)·얼마나 쓸모 있나(설명 충실도)"로 관리한다.

-->

삭제

9.2 고도화 로드맵 — 우리 수준과 다음 한 걸음 (성숙도 단계) --> 고도화 로드맵 (성숙도 단계)
날짜표가 아니라 수준으로 본다. "메타데이터를 누가·얼마나 자동으로 채우고 설명하는가" 기준으로, 「지금 우리가 이런 모습이면 = 이 수준」을 짚고 → 「다음 단계로 가려면 무엇을」 정한다.

-->

로드맵은 일정이 아니라 성숙도 수준을 기준으로 정의한다.

메타데이터를 누가·얼마나 자동으로 채우고 설명하는지를 기준으로 현재 수준을 진단하고 다음 단계로 발전하는 방향을 제시한다.

| 단계 | 이런 모습이면 = 이 수준 (진단 신호) | 다음 단계로 가려면 (다음 한 걸음) |

-->

| 단계 | 현재 상태(수준) | 다음 단계로의 진행을 위한 필수 요건 |

공통 원칙(순서가 핵심): 각 단계는 이전 단계가 안정화된 뒤 올라간다. 자동 수집(Lv2) 없이 AI 초안(Lv4)부터 올리면 근거 없는 설명이 양산돼 신뢰도가 떨어진다.

-->

공통 원칙: 각 단계는 이전 단계가 안정화된 뒤 순서대로 진행된다. 예를 들어 자동 수집(Lv2) 없이 AI 보조·확대(Lv4)로 진행 시, 근거 없는 설명이 양산돼 신뢰도가 떨어진다.

9.3 미래 AI 자동화 전망 — 어디는 AI가, 어디는 사람이 --> AI 자동화 가능 영역 및 사람 중심 영역
메타데이터 준비 중 기계적으로 채우고 풀이하는 일은 갈수록 AI가 대신하고, 의미·기준·책임을 정하는 판단은 사람이 끝까지 쥔다.

-->

메타데이터 준비 과정에서 반복적이고 정형화된 작업은 점차 자동화될 수 있다.

반면 메타데이터 의미 정의, 기준 설정, 책임과 같은 영역은 지속적으로 사람의 역할이 필요하다.
