# B-1 데이터 전처리 — 작업 메모 (주제 로컬 규칙·결정·피드백)

> 이 파일은 B-1 주제 전용 메모다. 본문 작성/수정 전 먼저 읽는다. 전역 규칙은 `CLAUDE.md`·`공통 규칙/`.

## 정본(canonical) 모델 — 끝까지 일관 유지할 것

**전처리 변환 사슬 4단계: ① 추출(Extract) → ② 구조화(Structure) → ③ 위치보존(Provenance) → ④ 청킹(Chunk).**
- 3장(무엇을)에서 이 4단계를 정의하고, 5장 시나리오·6장 구축 파이프라인(6단계: 대상선정+위 4단계+적재)·7장 경계까지 이 모델로 일관 재사용했다.
- 6단계 구축 파이프라인 = 4단계 사슬 앞에 "①대상 선정", 뒤에 "⑥적재"를 더한 것. (3장 4단계와 6장 6단계가 어긋나 보이지 않게 — 6.1에서 "③~⑤는 3장에서 갖춘 것의 실행"이라고 명시함.)

## 주제 경계 (MECE) — 가장 중요

- **B-1 = 형식 변환만** (이미 디지털인 자료를 AI가 읽을 구조로).
- 대량 디지털화·OCR = **F-3**, 의미(정답) 라벨 = **B-2**, 사용 가능 판정 = **C-2**, 근거 추적 = **C-3**.
- ★ 가장 헷갈리는 경계: **B-1 vs F-3** — F-3는 "디지털로 만들기(스캔·OCR)", B-1은 "구조화하기". 7장에서 강조 박스로 명시.
- 데이터 준비 관점 고정: 청킹·벡터DB·임베딩을 다루되 "RAG/AI 만드는 법"이 아니라 "검색 가능한 청크로 데이터를 준비"하는 관점. 본문에 *(임베딩 모델 만드는 게 아님)* 류 단서를 명시해 둠.

## KQ → 섹션 매핑 (합격 기준 자가 점검 — 5문항 전부 커버 확인)

| KQ | 질문 | 위치(anchor) |
|----|------|------|
| 1 | 어떤 데이터가 전처리 대상인가 | §4.1 `#kq1` |
| 2 | 문서 유형별로 무엇을 추출 | §3.2 `#kq2` |
| 3 | 어떤 구조로 변환 (구조화·청킹) | §3.3 `#kq3` |
| 4 | 양식 바뀌어도 안정적으로 | §3.4 `#kq4` (+ 운영 §6.4 `#sec64`) |
| 5 | AI 활용 환경에 어떻게 제공(적재) | §6.3 `#sec63` |

- 제목에 KQ 코드 노출 금지 → 상단 "핵심 질문 박스" + 각 절 `> ❓` 안내 + 명시적 `<a id>`로 처리.

## 섹션 구성 (00 목차의 B-1 8섹션 따름)

1 개요 · 2 왜(Why) · 3 무엇을(What) · 4 어디부터(대상·우선순위) · 5 예시 시나리오 · 6 어떻게(How) · 7 관계 · 8 지표·고도화.
- 예시(5장)는 How 앞에 두고 효과+흐름만. **맨 끝에 별도 계열사 예시 추가하지 않음**(중복 방지 규칙). 구축 절차는 6장에.
- 계열사 예시는 **두산에너빌리티 품질 검사 보고서 PDF**로 통일(시나리오·도구선택·골든셋 예시 모두 동일 계열사).

## 다이어그램 (취사 기준 적용)

만든 것: 1.3 체계 내 위치(선형, 표준), 3.1 변환 사슬(누적/순서), 4.2 우선순위(quadrant), 5.2 흐름(순서+루프), 6.1 6단계(순서+루프), 6.4 운영(분기/루프), 7.2 경계 조감도, 8.2 로드맵(gantt).
- 3.2 문서 유형별 추출 = **표로 처리**(단순 나열이라 다이어그램 X). 2장 Pain→해결도 다이어그램 X(불릿으로 충분 — 02 표준 §4-1).

## 도구·출처

- 도구 맵: 오픈소스(Docling/Unstructured/LlamaParse/Camelot/pdfplumber/PyMuPDF/python-pptx/openpyxl/Tika) + 클라우드(Azure DI/AWS Textract/Google Doc AI). 전부 공식 URL 인라인 링크 + References.
- 가격·버전·한국어 지원은 **단정 금지** — "PoC/공식문서 확인" 명시.
- AWS Textract 한국어 지원 범위 = 확인 필요로 표시함.
- 리서치 원자료: `research/research-A-what-tools.md`, `research/research-B-stability-serving-kpi.md`. 출처 검증: `research/verification.md`.

## 피드백 이력

- **2026-06-19 (v0.2)** 고객: "PPT·Excel 등 유형별로 어떻게 전처리하는지 디테일하게 들어가야".
  → §3.2를 유형별 4블록(3.2.1 PDF / 3.2.2 PPT / 3.2.3 Excel / 3.2.4 이미지)으로 확장. 각 블록에 **권장 처리 순서·함정·완성 예시**. PPT=슬라이드 구조화 JSON 예시, Excel=병합 풀기+교차표 wide→long 정규화 예시 추가. 깊은 라이브러리/코드 수준은 Backup 3-A 유지(깊이 2층).
- **2026-06-19 (v0.3)** 고객: "실질적으로 따라할 수 있게, 문서 유형별로(이메일 등) LLM·코드 예시 많이".
  → 설계 결정(사용자 선택): **"본문 간결 + 별첨에 실행 레시피"**. 추가 유형 4종: 이메일·Word/HWP·CSV/로그/센서·채팅/회의록.
  → §3.2: 개요 표에 4행 추가 + 소절 3.2.5~3.2.8 신설(권장 순서·함정만, 코드는 별첨으로 링크).
  → **[Backup 3-D] 유형별 실행 레시피** 신설 = 핵심 산출. 6개 레시피(PDF표·Excel정규화·이메일·Word/HWP·CSV/센서·채팅) 각각 `준비물→코드(또는 LLM)→입력→출력`. 라이브러리: pdfplumber/Camelot, openpyxl/pandas, email+talon/email-reply-parser, python-docx/mammoth+pyhwp(+LibreOffice fallback), pandas+charset-normalizer, re.
  → **원칙 고정**: (a) 언제 코드 vs LLM 가이드, (b) LLM은 "데이터 정돈 도구"(챗봇/RAG 만드는 게 아님 — 데이터 준비 관점 유지), (c) LLM 출력은 JSON 강제+원문 대조+값 지어내기 금지. (d) **채팅 "결정사항·요약 추출"은 B-2 경계**라고 레시피에 명시.
  → 주의: HWP 도구는 빈약하다고 정직하게 명시(pyhwp/HWPX unzip/LibreOffice→PDF 3경로). 라이브러리 URL은 References "유형별 실행 레시피 도구"에 추가 — 버전·API 변동 가능, 공식문서 재확인 단서 유지.
- **2026-06-19 (v0.4)** 고객: "내용적으로 더 디테일해야. 그냥 병합 풀기는 너무 간단한 거 아니야?".
  → Backup 3-D 6개 레시피 전면 심화 = happy-path 3줄 → **"실전 함정 표 + 그걸 처리하는 더 완성된 코드 + 검증(assert)"** 구조로 통일.
  → 유형별 추가 깊이: **Excel**(예시로 지적당한 것) — 머리글 행 탐지, 다중 행 머리글 평탄화, 머리글 병합(가로)vs데이터 병합(세로) 방향 구분, 수식 캐시값 None 대응, 색/조건부서식 차이(직접 색=fill / 조건부서식=규칙 평가 필요), 소계·단위 행 제거, 숫자 정제, 행 수 검증. **PDF** — 스캔 판별, 테두리없는 표 table_settings, 셀 줄바꿈, 페이지 넘김 머리글 제거, 열 수 검증, Camelot accuracy/table_areas. **이메일** — 인코딩 헤더, HTML→평문(html2text), top/bottom/inline 인용 talon, 서명 bruteforce 분리, 스레드(In-Reply-To/References), inline(Content-ID) vs 첨부. **Word** — 문서 순서 순회(iter_blocks), 병합셀 중복 텍스트, 변경추적; HWP 3경로 + 빈 결과 assert. **CSV/센서** — 머리글 탐지, 단위 행 제거, dtype=str 후 숫자 정제, 중복/역순/utc, chunksize. **채팅** — 다포맷 PATTERNS, ALIAS 정규화, 연속 발화 병합, .vtt(webvtt-py).
  → References에 html2text·formulas·webvtt-py 추가. 코드 API는 표준 라이브러리 기준으로 정확하게 작성했으나 버전 변동 가능 — 실제 적용 시 PoC 검증 권장.
  → 열린 점: 사용자가 "실제 계열사 양식 샘플"을 주면 레시피를 그 양식 기준 완성 샘플로 1건씩 구체화 가능(현재는 일반 패턴).

## 다음 작업 / 열린 항목

- 출처 검증(verification.md) 결과 반영 — 죽은/의심 URL 있으면 보정.
- 고객 피드백 받으면 하단 변경 이력 표에 누적 + 별도 커밋. status draft→review→confirmed.
