(현재) --> (수정)

### 1.2 기대 효과

데이터 카탈로그는 일회성 구축 과제가 아니다. AI 프로젝트에서 축적되는 데이터를 조직의 공통 자산으로 관리하고, 이후 프로젝트에서 반복 활용할 수 있도록 하는 기반 체계이다.

-->

데이터 카탈로그는 일회성 구축 과재가 아닌, AI 프로젝트에서 축적되는 데이터를 조직의 공통 자산으로 관리하고, 이후 프로젝트에서 반복 활용할 수 있도록 하는 기반 체계이다.


### 1.3 적용 전·후


데이터 카탈로그가 있기 전과 후의 업무 방식은 다음과 같이 달라진다.

-->

데이터 카탈로그 적용 전과 후의 업무 방식은 다음과 같다.


### 2.1 데이터 카탈로그란 + 체계 내 위치


관리 대상은 데이터 자체가 아니라 데이터를 설명하는 정보이다.

-->

데이터 카탈로그에서 관리하는 대상은 데이터 자체가 아닌, 데이터를 설명하는 정보이다.



의미의 순서와 구축의 순서는 다르다. 개념적으로는 표준 용어(A-3)와 자산의 속성(A-2)이 데이터의 의미를 채우고, 카탈로그(A-1)는 그렇게 의미가 채워진 자산이 "어디에 있는지"를 담는다(그래서 본 매뉴얼도 A-3 → A-2 → A-1 순으로 읽는다). 다만 실제 구축은 순서가 반대인 경우가 많다 — 무엇이 어디 있는지 목록(카탈로그)을 먼저 세운 뒤, 그 위에 메타데이터와 표준 용어를 연결해 의미를 채워 간다. 이런 의미에서 카탈로그는 정비의 출발점이자 기반이 된다.

-->

개념적으로는 표준 용어(A-3)와 자산의 속성(A-2)이 데이터의 의미를 채우고, 카탈로그(A-1)는 그렇게 의미가 정의된 자산이 어디에 있는지를 담는다(본 매뉴얼도 A-3 → A-2 → A-1 순으로 읽도록 구성). 다만 실제 구축은 이와 반대로, 먼저 무엇이 어디에 있는지 목록(카탈로그)을 구축한 뒤, 그 위에 메타데이터와 표준 용어를 연결해 의미를 단계적으로 채우는 경우가 많다. 이런 점에서 카탈로그는 AI-ready Data 체계 정비의 출발점이자 기반이 된다.


### 2.2 카탈로그 조회 방식


데이터는 검색과 탐색 두 가지 방식으로 조회한다.

검색은 데이터명이나 태그를 이용하여 필요한 데이터를 직접 찾는 방식이며, 탐색은 업무 영역과 시스템, 데이터 유형 등을 기준으로 범위를 좁혀가는 방식이다.

실제 운영에서는 두 방식을 함께 사용한다.

-->

데이터 카탈로그 내에서 데이터 조회 방식은 검색과 탐색으로 가능하며, 실제 운영에서는 두 방식을 함께 사용한다.

검색은 데이터명이나 태그를 이용하여 필요한 데이터를 직접 찾는 방식이며, 탐색은 업무 영역과 시스템, 데이터 유형 등을 기준으로 범위를 좁혀가는 방식이다.



### 2.4 기본 등록 항목 --> 대표 등록 항목


자산 1건을 등록할 때 필요한 최소 정보 집합이다. 데이터가 어디에 존재하며, 누가 관리하고, 어떤 절차로 접근하는지를 기록한다. 필수는 등록을 위해 반드시 필요한 정보이고, 선택은 탐색·활용 편의를 높이는 정보이다. 작성 주체는 자동 수집 항목과 사람이 보완하는 항목으로 나뉜다.

기본 등록 항목만으로도 필요한 데이터의 위치와 관리 정보를 확인할 수 있어야 한다. 전체 등록 항목 사전은 Appendix A에서 확인할 수 있다.

-->

데이터 카탈로그에 1건의 자산을 등록할 때 필요한 최소 정보 집합으로, 데이터 위치, 관리 주체, 접근 절차 등을 기록하며, 기본 등록 항목만으로도 필요한 데이터의 위치와 관리 정보를 확인할 수 있어야 한다. 필수는 등록을 위해 반드시 필요한 정보이고, 선택은 탐색·활용 편의를 높이는 정보이다. 작성 주체는 자동 수집 항목과 사람이 보완하는 항목으로 나뉜다.

전체 등록 항목 사전은 Appendix A에서 확인할 수 있다.



### 2.5 데이터 분류 기준

데이터는 동일한 기준으로 분류되어야 검색과 재사용이 가능하다.

데이터 카탈로그에서는 다음 기준으로 데이터를 분류한다.

이 분류 기준은 검색 조건과 태그 체계의 기준으로 함께 활용한다.

-->

데이터는 동일한 기준으로 분류되어야 검색과 재사용이 가능하며, 분류 기준은 검색 조건과 태그 체계의 기준으로 함께 활용한다.

데이터 카탈로그에서는 다음 기준으로 데이터를 분류한다.



### 2.7 태그 표준값 목록


태그는 데이터 검색과 분류 기준을 통일하기 위한 표준값이다.

동일한 데이터를 사람마다 다른 표현으로 등록하면 검색 품질이 낮아질 수 있다. 따라서 자유 입력보다 표준값 사용을 원칙으로 한다.

표준 태그를 적용하면 계열사 전체에서 동일한 기준으로 데이터를 검색하고 활용할 수 있다.

-->

태그는 데이터 검색과 분류 기준을 통일하기 위한 표준값으로, 검색 품질의 하락을 방지하기 위해 동일한 데이터에 대해 자유 입력보다 표준값 사용을 원칙으로 한다. 이를 통해 계열사 전체에서 동일한 기준으로 데이터를 검색하고 활용할 수 있게 한다.


### 3.1 등록 대상 범위


등록 대상은 AI 활용 가능성과 업무 중요도를 기준으로 선정한다.

다음 조건 가운데 두 가지 이상에 해당하는 데이터는 우선 등록 대상으로 선정한다.

-->

등록 대상은 AI 활용 가능성, 업무 중요도, 재사용성을 기준으로 선정하며, 다음 조건 가운데 두 가지 이상에 해당하는 데이터는 우선 등록 대상으로 선정한다.

### 3.2 유형별 등록/제외


데이터 유형에 따라 등록 기준을 달리 적용한다.

개인 임시 파일·테스트 데이터·중복 데이터·폐기 예정 데이터는 우선 등록 대상에서 제외하거나 후순위로 관리한다.

-->

데이터 유형에 따라 등록 기준을 달리 적용하여 개인 임시 파일·테스트 데이터·중복 데이터·폐기 예정 데이터는 우선 등록 대상에서 제외하거나 후순위로 관리한다.

### 3.4 수집·등록 방식 (자동 vs 수동)


데이터는 가능한 자동으로 수집·등록한다.

시스템에서 메타데이터를 수집할 수 있는 경우에는 자동 등록을 적용하고, 자동 수집이 어려운 데이터만 수동으로 등록한다.

자동 등록을 우선 적용하면 구축 기간을 줄일 수 있고, 이후 변경 사항도 자동으로 반영할 수 있다.

-->

데이터는 가능한 한 자동으로 수집·등록하며, 시스템에서 메타데이터를 수집할 수 있는 경우에는 자동 방식을 적용하고 자동 수집이 어려운 데이터만 수동 방식을 적용한다. 자동 수집·등록의 우선 적용을 통해 구축 기간을 줄이고 이후 변경 사항을 자동으로 반영할 수 있다.




| 등록 방식 |

| 자동 등록 |

| 수동 등록 |

-->

| 수집·등록 방식 |

| 자동 |

| 수동 |

### 3.5 보안 검토 (내부 학습 vs 외부 LLM 활용)


등록 대상은 활용 목적에 따라 보안 기준을 함께 검토한다.

데이터 카탈로그는 데이터 자체를 저장하지 않는다.

필요한 경우 데이터 위치와 관리 정보만 등록하고, 실제 데이터 접근은 별도의 승인 절차를 적용한다.

-->

데이터 카탈로그는 데이터 자체를 저장하지 않으므로, 데이터 위치와 관리 정보만 등록하고, 실제 데이터 접근은 별도의 승인 절차를 적용한다.

등록 대상은 활용 목적에 따라 보안 기준을 함께 검토한다.



### 3.6 최종 우선순위 — 사람이 다 찾아 등록하지 않는다 --> 최종 우선순위


데이터 카탈로그 구축은 사람이 모든 데이터를 직접 등록하는 작업이 아니다.

자동 수집이 가능한 데이터는 시스템에서 등록하고, 사람이 판단해야 하는 정보만 보완하는 방식으로 운영한다.

-->

데이터 카탈로그 구축은 사람이 모든 데이터를 직접 등록하는 작업이 아니라, 자동 수집이 가능한 데이터는 시스템에서 등록하고 사람이 판단해야 하는 정보만 보완하는 방식으로 운영한다.




| 자동 등록 | 담당자 등록 |

-->

| 자동 등록 | 수동 등록 |




데이터가 증가할수록 사람이 직접 등록하는 방식은 유지하기 어렵다.

자동 수집을 기본으로 하고, 데이터 오너가 필요한 정보만 검토·보완하는 구조를 구축해야 지속적으로 운영할 수 있다.

-->

데이터가 증가할수록 사람이 직접 등록하는 방식은 유지하기 어려우므로 자동 수집을 기본으로 하고, 데이터 오너가 필요한 정보만 검토·보완하는 구조를 구축해야 지속적으로 운영할 수 있다.



등록 대상은 AI 활용도와 업무 중요도 두 축으로 배치해 등록 순서(Wave)를 정한다. 두 축이 모두 높은 자산을 먼저 등록하고, 한쪽만 높은 자산은 다음 차수로, 둘 다 낮은 자산은 후순위로 둔다.

-->

삭제



사분면 그래프 및 Wave 123 표

-->

삭제



### 4.1 등록 기준 정의


| Before (이렇게 쓰면) | After (이렇게) | 왜 |

-->

| 변경 전 | 변경 후 | 변경 사유 |

### 4.4 Legacy 연동 및 Pipeline 구축


§3.4에서 자동·수동으로 가른 대상에 맞춰 실제 수집 경로를 구축한다.

-->

§3.4에서 자동·수동으로 구분한 대상에 맞춰 실제 수집 경로를 구축한다.



자동 수집 파이프라인 — ERP·MES·QMS·LIMS처럼 스키마를 API·JDBC로 노출하는 시스템은 커넥터를 붙여 정기·증분 방식으로 메타데이터를 수집한다. 한 번 연결해 두면 신규 테이블·컬럼과 갱신 정보가 사람 손을 거치지 않고 따라 들어온다. 첫 연결 때는 전체 스캔, 이후에는 변경분만 증분 수집해 부하를 줄인다.

수동 등록 경로 — SharePoint·파일 서버·개인 관리 문서처럼 자동 수집이 어려운 자산은 등록 양식(데이터명·위치·오너·보안 등급)으로 받아 카탈로그에 올린다. 양식을 표준화해 두면 수동 등록도 같은 품질로 유지된다.

-->

자동 수집 파이프라인 — ERP·MES·QMS·LIMS처럼 스키마를 API·JDBC로 노출하는 시스템은 커넥터를 붙여 정기·증분 방식으로 메타데이터를 수집한다. 첫 연결 때는 전체 스캔, 이후에는 변경분만 증분 수집해 부하를 줄일 수 있으며, 한 번 연결해 두면 신규 테이블·컬럼과 갱신 정보가 사람 손을 거치지 않고 따라 들어온다.

수동 등록 경로 — SharePoint, 파일 서버, 개인 관리 문서와 같이 자동 수집이 어려운 자산은 표준화된 등록 양식 (데이터명, 위치, 오너, 보안 등급) 받아 카탈로그에 등록하며, 이를 통해 수동 등록도 일관된 품질로 유지한다.




### 4.6 담당자 역할
데이터 카탈로그는 특정 조직만으로 운영할 수 없으며 데이터 오너를 중심으로 여러 조직이 역할을 분담한다.

각 조직은 자신의 역할에 따라 등록, 검토, 운영을 수행하며 데이터 오너가 전체 운영을 총괄한다.

-->

데이터 카탈로그는 특정 조직만으로 운영할 수 없으므로 데이터 오너가 전체 운영을 총괄하며, 데이터 오너를 중심으로 여러 조직이 등록, 검토, 운영을 수행하는 역할을 분담한다.



### 4.7 실제로 어디서 채우나 — 플랫폼 매핑
등록 항목을 실제로 어디서 채우는지는 솔루션마다 다르지만 원리는 같다. 기술·운영 항목은 솔루션의 자동 수집 기능이 원천 시스템에서 직접 끌어오고, 사람은 데이터의 의미·보안 등급처럼 판단이 필요한 항목만 화면에서 보완한다. 즉 "어디서 채우나"의 답은 대부분 "사람이 입력하는 곳"이 아니라 "시스템이 자동으로 채우는 곳"이다.

-->

등록 항목을 실제로 어디서 채우는지는 솔루션마다 다르지만 원리는 같다. 기술·운영 항목은 솔루션의 자동 수집 기능이 원천 시스템에서 직접 끌어오고, 사람은 데이터의 의미·보안 등급처럼 판단이 필요한 항목만 화면에서 보완한다.




| 등록 항목 | 어디서 채우나 (예) | 누가 |

-->

| 등록 항목 | 등록 방법 (예) | 등록 주체 |

### 5.2 후보 검토·기능 비교


두산전자 환경(클라우드 데이터 레이크 + Oracle/MS SQL + SharePoint, 데이터 조직 1년차)을 가정한 후보 3종 비교 예시는 다음과 같다. 별점 단정 대신 연계 가능 여부를 세 단계(연계 가능 / 요확인 / 불가)로 표기한다. 실제 연계 여부와 버전별 기능 범위는 반드시 공식 문서와 PoC로 확인한다.

-->

두산전자 환경(클라우드 데이터 레이크 + Oracle/MS SQL + SharePoint, 데이터 조직 1년차)을 가정한 후보 3종 비교 예시는 다음과 같다.

### 5.3 평가·선정·PoC 기준


솔루션은 기능만으로 선정하지 않는다.

실제 운영 환경에서 PoC를 수행하여 연계성과 운영성을 함께 검증한다.

PoC 결과를 바탕으로 기능, 운영성, 확장성을 종합적으로 검토하여 최종 솔루션을 선정한다.

-->

솔루션은 실제 운영 환경에서 PoC를 수행하여 연계성과 운영성을 함께 검증하며, PoC 결과를 바탕으로 기능, 운영성, 확장성을 종합적으로 검토하여 선정한다.



## 6. Where — 다른 주제와의 관계


데이터 카탈로그는 AI-ready Data 체계에서 데이터를 찾는 역할을 담당한다.

데이터의 의미를 정의하거나 품질을 관리하는 것이 아니라, 필요한 데이터를 찾고 활용할 수 있도록 연결하는 역할을 수행한다.

-->

데이터 카탈로그는 AI-ready Data 체계에서 데이터의 의미 정의나 품질 관리를 담당하는 것이 아닌, 필요한 데이터를 찾고 활용하도록 연결하는 역할을 수행한다.



### Appendix A. 등록 항목 사전 (전체)
본문 §2.4는 현업이 먼저 보는 대표 항목만 담았다. 아래는 자산 1건을 등록할 때 채우는 전체 항목 사전이다.

-->

본문 §2.4에는 현업이 우선 확인해야 하는 대표 항목만 수록했으며, 아래는 자산 1건을 등록할 때 입력하는 전체 항목 사전이다.



필수 항목(데이터명·보유 시스템·저장 위치·데이터 유형·설명·업무 영역·관리 조직·데이터 오너·보안 등급·개인정보·접근 방법)만 채워도 "어디에 무엇이 있고 누구에게 어떻게 요청하나"를 확인할 수 있다. 나머지는 탐색·재사용 편의를 높이는 선택 항목이다.

-->

필수 항목만 입력해도 “어디에 무엇이 있고 누구에게 어떻게 요청하나”를 확인할 수 있으며, 나머지 항목은 탐색과 재사용의 편의성을 높이기 위한 선택 항목이다.
